# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>" (a fixture copied into the oracle's target/).
# Optional lines: ref=, paths=, build=, include_tests=. Dev-branch only.
#
# Blind cross-tool run on a logging library — file/rolling sinks wrap
# StreamWriter/TextWriter and buffers (resource-heavy paths). Higher chance of a
# real oracle-only recall finding than a pure data lib; triage into field-notes
# per the oracle.md maintenance requirement.
serilog/serilog
