# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>" (a fixture copied into the oracle's target/).
# Optional lines: ref=, paths=, build=, include_tests=. Dev-branch only.
#
# Cross-tool oracle on a Linux-buildable fixture, so ALL THREE tools run (Infer#
# included — ScreenToGif's WPF won't build on Linux). Re-run for the EXCEPTION-EDGE
# slice: leak #4 (Dispose() inside try, after a may-throw call -> skipped on the throw
# path) should now land in "Agree" with CodeQL's cs/dispose-not-called-on-throw, where
# Own.NET used to miss it. Expect #2/#3/#4 in "Agree", #1 Own.NET-only. See the README.
local:corpus/fixtures/systemevents-console
build=SystemEventsLeak.csproj
