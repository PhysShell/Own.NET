# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>" (a fixture copied into the oracle's target/).
# Optional lines: ref=, paths=, build=, include_tests=. Dev-branch only.
#
# Blind run, 2026-06-26: a FRESH external target with no documented baseline, to
# see whether Own.NET surfaces any own-only (candidate false positives) or
# oracle-only (recall gaps) on real code it hasn't been tuned against. Polly v8
# targets net8/net6/netstandard2.0 only -> builds cleanly on the dotnet-8 runner,
# so all three tools (own-check + CodeQL + Infer#) should fire. Real resource
# surface: CancellationTokenSource, timers, IDisposable resilience pipelines.
# No expectation pinned — this is exploratory; read the report, don't assert it.
App-vNext/Polly
