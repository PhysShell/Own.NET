# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>". Optional: ref=, paths=, build=, include_tests=.
#
# 2026-06-23: cross-tool check of the Serilog mine's single finding —
# BatchingSink._shutdownSignal (a CancellationTokenSource FIELD, cancelled but never
# disposed). Hypothesis: own-only, because CodeQL's leak queries are LOCAL-scoped
# (cs/local-not-disposed) and Infer#'s RESOURCE_LEAK is METHOD-scoped, while our
# field-disposable detector targets owner-lifetime field leaks. This run confirms
# (or refutes) that the CTS-field TP is our differentiating capability, on real
# cross-tool output. Serilog builds clean on Linux (netstandard2.0/net8.0).
serilog/serilog
paths=src
build=src/Serilog/Serilog.csproj
