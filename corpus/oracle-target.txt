# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>" (a fixture copied into the oracle's target/).
# Optional lines: ref=, paths=, build=, include_tests=. Dev-branch only.
#
# TEMPORARY (revert before any PR): fresh-repo oracle sweep for new FP classes / recall gaps.
# Target 1 (done, clean): DapperLib/Dapper — 0 own-only; oracle-only = Infer# WrappedBasicReader
#   ownership-transfer FPs + CodeQL dispose-on-throw honest-skips. No new FP class.
# Target 2: StackExchange/StackExchange.Redis — surfaced the pooled-buffer-transfer-via-escaping-
#   local FP class (Lease.cs:42, RedisServer.cs:917). RE-RUN to confirm the WrapperLocalEscapes fix
#   clears those 4 own-only findings (expect Lease.cs / RedisServer.cs `keys`/`arr` gone from own-only).
StackExchange/StackExchange.Redis
