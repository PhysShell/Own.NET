# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>" (a fixture copied into the oracle's target/).
# Optional lines: ref=, paths=, build=, include_tests=. Dev-branch only.
#
# Real external repo, 2026-06-26: re-run the documented Dapper worked example on a
# live clone AFTER the P-014 Tier A/B semantic-resolution work, to confirm the
# cross-tool pipeline (clone -> build -> own-check + CodeQL + Infer#) still holds on
# external code. Documented baseline (oracle.md): Own.NET 0 / Infer# 3 / CodeQL 2
# leak-class, agree 0, own-only 0 (no false positives) on product code
# (--exclude-tests drops the test/benchmark findings). Infer#'s 3 are over-reports
# on Dapper's caller-owned DbWrappedReader (ownership transfer Own.NET treats as no
# leak); CodeQL's 2 are dispose-on-throw, an exceptional-CFG recall gap we skip by
# design. Expect: agree 0, own-only 0, oracle-only ~5 (the two named classes).
DapperLib/Dapper
