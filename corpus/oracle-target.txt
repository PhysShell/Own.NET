# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>" (a fixture copied into the oracle's target/).
# Optional lines: ref=, paths=, build=, include_tests=. Dev-branch only.
#
# TEMPORARY (revert before any PR): confirm the using-statement local-release fix
# (onThrowDefinite / the using(existingLocal) branch) clears the protobuf-net
# `assorted/ Silverlight Page.xaml.cs` `timer` FP. With the baseline still present,
# the finding must be ABSENT from BOTH "Own.NET only" AND "Known FP (baselined)" —
# that proves the extractor cleared it (not the baseline hiding it). include_tests
# so the assorted/ sample tree is not filtered out of the diff.
protobuf-net/protobuf-net
include_tests=true
