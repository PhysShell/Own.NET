# Target for the push-triggered oracle (.github/workflows/oracle.yml, push path).
# Format = mine-target.txt: first non-comment line is "owner/repo", an https URL,
# or "local:<in-repo path>". Optional: ref=, paths=, build=, include_tests=,
# body_throw_edges=.
#
# 2026-06-23 CAPSTONE: re-run the Npgsql v8.0.9 oracle WITH --body-throw-edges (PR #95's
# opt-in body-level dispose-not-called-on-throw firehose, now in main and plumbed into
# own-check here via body_throw_edges=). Clean before/after against the documented
# baseline (Own.NET 10 / CodeQL 19 / Infer# 73, Agree 0): Npgsql is rich in `new`-acquired
# disposable locals (converters / readers), so the firehose should (a) recover recall —
# move some of CodeQL's cs/dispose-not-called-on-throw findings into AGREE — and (b) add
# CA2000 noise (own-only jumps). This run measures BOTH halves of that tradeoff, which is
# exactly why the tier is opt-in. Same scope/ref as the baseline so the diff is the flag.
npgsql/npgsql
ref=v8.0.9
paths=src
build=src/Npgsql/Npgsql.csproj
body_throw_edges=true
