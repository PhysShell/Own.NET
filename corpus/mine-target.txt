# Target for the push-triggered miner (.github/workflows/mine-on-push.yml).
# First non-comment line: a GitHub "owner/repo" or an https git URL. One repo/run.
# Optional lines: ref=<branch|tag|sha>, paths=<subdir of the target to scan>.
# Bump this file (commit + push) to kick off a mine; read the result in the run log.
# 2026-06-23: fresh domain after StackExchange.Redis (#93). serilog/serilog — structured logging:
# sinks / async wrapper / periodic batching / buffers (IDisposable + async surface), almost pure
# BCL so it resolves cleanly on the Linux runner. Stresses field-disposable + flow-locals + pool.
# Scoped to src/ (the library) to skip test-code noise.
serilog/serilog
paths=src
