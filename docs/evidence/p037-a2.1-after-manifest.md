# P-037 A2.1 zero-semantic-cut evidence (after-run at A)

T = `4a8e6582e10222403cd40adc9e95db7e0228a1c2` (terminal-green baseline commit)
R = `5fd6bfa6abd4c2af7e53712af300cf66c97f2f50` (baseline evidence commit, on
    `claude/p037-a2-baseline-4a8e658`, not an ancestor of A — not rebased on)
A = `dce26ed1f5d831e36f78f02e307b8b772c8a8800` (A2.1 treatment: emits the
    guarded-fact sidecar from the Roslyn extractor, validated and inert;
    branch `claude/p037-a1-production`)
M = `P037_A2_MEASUREMENT_M1`, the same preserved measurement environment
    used for the T baseline (CPython 3.11.15, .NET SDK 8.0.425, Rust/Cargo
    1.94.1); same workspace checkout root reused, not a fresh worktree.

This commit (S) is evidence-only, built directly on A, not on R: A stays at
its own SHA so the treatment identity already measured is not disturbed.
The four T-baseline artifacts already committed in R are not duplicated
here.

## Four after-takes at A, population held at T

| artifact | sha256 | compare result |
|---|---|---|
| p037-a2.1-after-mos-repo.json | `af4bdd1a1b61032604b7726301dee33b4c7211f8d913d9f704c84fdc4108728d` | UNCHANGED, facts_moved=1, python_mos_moved=0, rust_mos_moved=0, after_parity_moved=0 |
| p037-a2.1-after-mos-corpus.json | `06e5775f2a5cd7f1fa7f36d0401730ba192960986be5f3cc35ef1cacfcd9e40b` | UNCHANGED, facts_moved=36, python_mos_moved=0, rust_mos_moved=0, after_parity_moved=0 |
| p037-a2.1-after-verdict-python.json | `886edbe07226ac940c04fa7a89f7b8c8775ce1eb360a5483e0683fd36fc30822` | UNCHANGED, no verdict moved (137 files, level=verdict) |
| p037-a2.1-after-verdict-rust.json | `35ebb6392db8c85e595ee52414a62cd0885a44d574a10fbf7f8f4d6f63661147` | UNCHANGED, no verdict moved (137 files, level=verdict); own-cli qualified sha == executed sha, post_run_intact=true |

Compared against the T-baseline artifacts recorded in R (sha256:
`bd07e87fee7c5d9607acd3c267fec39424dee1e51662a7bba1e4949c7c5837dd`,
`49f788f961b46f7884cde5bb06e2d35525e8ce27731d98bb5588dc550478db0b`,
`04c2fd6c75493bf90e36ac0c6c8acfbf7e692ab9a20a9d3a5591eb9be5be2d36`,
`b9271bf7c4de111d5632a544f8d6d0710a6612aa36f682ea3a2b29dda71ac78e`
respectively, all four `verify`d fresh against A before comparison).

All four: `is_evidence=true`, `source_commit=A`, `population_commit=T`,
`dirty=false`, `post_run_population_intact=true`.

## Execution profile equality

`p037_evidence.py profile` output is byte-identical between the T-baseline
capture and this after-run: CPython 3.11.15 (main, Sep 19 2026,
00:18:08), .NET SDK 8.0.425 (runtimes Microsoft.AspNetCore.App 8.0.31 +
Microsoft.NETCore.App 8.0.31), rustc/cargo 1.94.1. The `own-cli` qualified
binary sha256 is also unchanged from T (`8373f5f5...`), consistent with
A2.1 touching only the Roslyn extractor.

## Known pre-existing observation (not fixed here)

`scope_cache_sites[].file` in the raw facts carries an absolute path
rather than a repo-relative one — a pre-existing portability defect from
T, not introduced by A. It does not affect the measured MOS/verdict
surface and is not visible in this M1 comparison because the preserved
workspace root was reused for both the baseline and after takes. Left
unfixed here deliberately: this is an evidence-only commit, and fixing an
extractor path-handling defect belongs in a normal code change with its
own FACT-DIFF, not folded into a baseline/after comparison record.

Taken: 2026-09-19 (UTC), operator run.
